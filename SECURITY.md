# 敏感信息与问题反馈

不要通过公开 Issue、PR、提交或截图发送 API key、认证 URL、cookie、完整会话记录、用户配置或私人代码。复现路径使用占位值，错误只保留必要的状态码及脱敏片段。

如果意外公开凭据，先在对应服务撤销或轮换，再联系仓库维护者处理传播内容；仅删除当前文件不能清除 Git 历史。不要在后续报告里复述该凭据。

涉及本项目安全问题时，若仓库提供私密漏洞报告入口则使用该入口；没有时只提交不含敏感细节的联系请求。不要把公开 Issue 当成安全的凭据交换渠道。

本 skill 的预期模式是 dsh 完全访问权限，不提供项目目录沙箱保证。请审阅任务范围、来源代码及权限；操作系统和执行工具的限制仍然生效。
